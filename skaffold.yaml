apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/ryandawsonuk/jximportest:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/ryandawsonuk/jximportest:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: jximportest
        chartPath: charts/jximportest
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/ryandawsonuk/jximportest"
          image.tag: "{{.DIGEST_HEX}}"
